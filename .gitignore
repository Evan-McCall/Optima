# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/

# Env / secrets
.env

# Generated by `optima ingest` into the demo store (keep committed demo data clean)
demo_data/ingested.json

# Real, private workspace: never commit a team's actual experiments/docs.
# Only the README and the .gitkeep placeholder are tracked.
/data/*
!/data/README.md
!/data/.gitkeep

# uv
# (uv.lock is committed on purpose for reproducible installs)

# OS / editor
.DS_Store
.idea/
.vscode/
